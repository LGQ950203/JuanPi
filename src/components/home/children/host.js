import React,{Component} from "react";
import { Link } from "react-router-dom";

export default class Host extends Component  {
    render(){
        let {hostHost} = this.props;
        return (
            <div id="home_host">
                {
                    
                }
            </div>
        )
    }
}